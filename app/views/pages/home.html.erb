
<h1>Pages#home</h1>>
<%= link_to 'Login/Sign up', new_user_session_path %>
<br>

<%= render 'banner' %>

<h2 class="text-center mt-3">Boats near you</h2>
  <div class="cards-grid p-5">
    <% @boats.each do |boat| %>
        <div class="card-boat">
          <img src="https://images.unsplash.com/photo-1576638994386-8bce04e2f4cb?  ixid=MXwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHw%3D&ixlib=rb-1.2.1&auto=format&fit=crop&w=651&q=80"/>
          <div class="card-boat-infos">
            <div>
              <h2><%= boat.name %></h2>
              <p><%= boat.description %></p>
            </div>
            <h4>€<%= boat.price %>/day</h4>
          </div>
        </div>
    <% end %>
  </div>

<%= link_to 'All boats', boats_path %>
<br>

<% if current_user %>
  <% if current_user.host %>
    <%= link_to "create boat", new_boat_path %>
  <% end %>
<% else %>
  <%= link_to 'Login/Sign up', new_user_session_path %>
<% end %>

